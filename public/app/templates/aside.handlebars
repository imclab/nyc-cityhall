<div class="row">
  <div class="grid-2 grid-md-5 grid-sm-8">
    <ul>
      {{#each agencies}}
        <li>{{name}}</li>
      {{/each}}
    </ul>

    <ul>
      {{#each types}}
        <li>{{name}}</li>
      {{/each}}
    </ul>
  </div>
</div>
